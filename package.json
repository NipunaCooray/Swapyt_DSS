{
  "name": "swapyt-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:rules": "node scripts/validate-rules.mjs",
    "build": "npm run validate:rules && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
